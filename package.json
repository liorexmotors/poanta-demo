{
  "name": "poanta-demo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "vite --host 0.0.0.0",
    "build": "rm -rf dist && mkdir -p dist && cp index.html studio.html feed.json candidates.json rss_sources.json sw.js manifest.webmanifest icon-192.png icon-512.png dist/",
    "quality": "python3 scripts/pointa_quality_gate.py --report pointa_quality_report.md"
  },
  "devDependencies": {}
}
