{
  "name": "poanta-demo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "vite --host 0.0.0.0",
    "build": "rm -rf dist && mkdir -p dist && cp index.html feed.json sw.js manifest.webmanifest icon-192.png icon-512.png dist/"
  },
  "devDependencies": {}
}
